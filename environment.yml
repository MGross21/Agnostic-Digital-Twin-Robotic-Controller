name: agnostic-controller
channels:
  - conda-forge  # Preferred channel for compatibility
  - defaults  # Default channel for basic packages
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - pip  # Ensures pip is available for packages not on Conda
  - pip:
      # - tensorflow
      # - gym
      - pybullet
      - scipy
      - ultralytics # for YOLOv11
